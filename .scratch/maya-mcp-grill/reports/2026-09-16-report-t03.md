# T-03 交付报告 — checkpoint/rollback 重做（D-007, D-015, D-016）

> 日期: 2026-09-16 · 分支: fix/t03-checkpoint-rollback · commit: lkt
> 基线(重跑口径): pytest 335+2skip; ruff src=185 全仓=244; mypy=227

## 交付声明与可复跑证据

### 1. checkpoint = exportAll 内存态真快照（P0-3 根除）
- 旧实现 `shutil.copy2(scene_path)` 复制磁盘旧文件 → 改为 `cmds.file(cp_path, exportAll=True, type="mayaAscii", force=True, prompt=False)`（maya_scene_module.py _write_snapshot）
- 证据: `pytest tests/test_checkpoint_rollback.py -q` → 31 passed；其中 test_snapshot_captures_memory_not_stale_disk 断言快照含内存中新节点（磁盘文件为陈旧内容），test_snapshot_when_scene_file_missing_on_disk 断言场景文件不在磁盘时仍快照成功（copy2 实现必败）

### 2. rollback = S2 重绑 + 结构化场景身份
- 打开快照后 `cmds.file(rename=scene_before, prompt=False)` 重绑原路径；返回 scene_name_before/after、original_file_status(saved|no_original_file)、scene_rebound_to
- 证据: test_rollback_s2_rebind_structured、test_rollback_restores_snapshot_state（stub open 真恢复场景图，断言 GEO_after 消失、位移还原）

### 3. 不变式：覆盖路径名前必有内存态快照
- auto_before_rollback 改 exportAll（cp_auto_before_rollback_{ts}[_{n}].ma）；失败即中止（aborted=True 结构化 error），discard_current_state=True 逃生 → safety_snapshot="skipped_by_user"；auto 成功却传 discard → warning
- 证据: test_rollback_auto_snapshot_captures_current（auto 快照含 post-checkpoint 修改）、test_auto_snapshot_failure_aborts（无 open 调用记录）、test_discard_current_state_escape、test_discard_flag_warns_when_auto_ok

### 4. name 严格白名单 + 同名拒绝/覆盖保留
- ^[A-Za-z0-9_-]+$ 拒绝带 suggestion（不自动采用）；cp_{name}.ma 同名默认拒绝，overwrite=True 时旧件 rename 为 prev_{ts}_{file}（preserved_as 披露，可 rollback）
- 证据: test_name_whitelist_rejects/edge_chars/suggestion_not_auto_applied、test_same_name_rejected_by_default、test_overwrite_preserves_old_file、test_rollback_to_preserved_prev_file

### 5. untitled → ad-hoc 快照（D-015/D-016）
- 默认报错（自愈文案）；显式 name → cp_adhoc_{name}.ma 落 <workspace>/checkpoints/；original_file_status=no_original_file、scene_rebound_to=null；rollback 走 S1 停留 cp 路径
- 证据: test_untitled_default_errors、test_untitled_adhoc_snapshot、test_rollback_untitled_stays_on_cp、test_list_untitled_uses_workspace

### 6. 隐藏案例 1-4（D-015 必测）
- ① checkpoints 为文件/不可写 → 预检错误透传：test_checkpoints_path_is_file、test_checkpoints_dir_unwritable
- ② 同名 cp → test_same_name_rejected_by_default + test_overwrite_preserves_old_file
- ③ 快照外部删改 → exists 预检 + //Maya ASCII 头校验，报“快照已丢失”：test_rollback_missing_snapshot、test_rollback_corrupt_snapshot
- ④ open 失败半残态 → scene_name_after + scene_snapshot 重建建议：test_rollback_open_failure_reports_state

### 7. '..' 穿越修复
- filename 白名单 ^(cp|prev)_[A-Za-z0-9_-]+\.ma$ + basename + commonpath 目录内校验
- 证据: test_rollback_rejects_traversal（../x.ma、..\x.ma、sub/cp_x.ma、cp_../../x.ma 全拒）、test_rollback_rejects_non_checkpoint_name

### 8. prompt=False / absoluteName 消歧
- 全部 mutating file 调用带 prompt=False；sceneName 查询走 expandName 优先
- 证据: test_file_ops_use_prompt_false、test_rollback_file_ops_prompt_false、test_scene_name_uses_absolute_name

### 9. 工具面 + 文档同步
- scene_checkpoint(name=None, overwrite=False)、scene_rollback(filename, discard_current_state=False)；server.py instructions 更新；README×2 删“自动回滚”虚词改诚实表述；AGENTS.md tests 表补全（偿还登记债）；ledger D-016 落账；ADR-0004 refined-by+consequence；CONTEXT.md ad-hoc 位置
- 证据: test_scene_tools_json.py 3 个新用例（overwrite/discard kwarg 透传、name=None 默认）

### 10. stub 升级（测试基础设施）
- maya_stub/cmds.py: exportAll 写真文件（//Maya ASCII 头 + JSON 场景载荷）、open 真恢复+坏文件抛错、expandName/absoluteName 查询、workspace()；scene.py: serialize/restore、workspace_dir
- 证据: tests/test_maya_stub.py 既有用例仍绿（stub 数学未动）

## 验收标准核对

| 标准 | 证据 |
|------|------|
| 编译通过 | `python -m compileall -q src tests` → OK（仅 test_mayapy_smoke.py 存量 SyntaxWarning） |
| 打包通过 | `python -m pip wheel . --no-deps -w dist_audit` → maya_mcp_server-0.1.0-py3-none-any.whl |
| 启动并测活 | stdio initialize→2.14.2/proto 2024-11-05；tools/list→18 tools；ping→{}；scene_checkpoint schema 含 name/overwrite，scene_rollback 含 filename/discard_current_state |
| test 闭环 | `pytest tests/ -q` → 369 passed + 3 skipped（基线 335+2 → +34 新用例）；新增 31 模块级 + 3 工具层 + 1 mayapy 档（reference-edit 往返，manual tier 标记） |
| 预算只降不升 | ruff src 185→185、全仓 244→244（不升）；mypy 227→223（降） |

## 遗留/边界（诚实声明）

- mayapy 档 reference-edit 往返用例已写但**本机无 Maya 未实跑**——manual tier，须在有 Maya 的机器上执行 `mayapy -m pytest tests/ -m mayapy`
- prev_* 保留件不进 list_checkpoints（count 只计 cp_*.ma），但文件名可经 preserved_as 拿到并直接 rollback
- 多 session 对同一场景文件 rollback 会互踩 rebind——文档已标“单场景文件单会话”假设
- untitled 场景 workspace 不可解析时 ad-hoc 快照报结构化错误
