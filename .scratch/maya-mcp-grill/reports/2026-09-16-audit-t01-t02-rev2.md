# AUDIT REV2 — T-01/T-02 (commit 63dfeae, change-id kzr, branch fix/t01-t02-stub-and-p0)

## 判定: 通过(带登记残留)

返工项全部复核为真,硬验收重跑全绿。

### 必修复核
- ghost 守卫: check_constraints 921/956 双处 "error" not in m;test_errored_measure_no_ghost_violation 在 v1(fd1b557) 复现 RED、当前 GREEN — 真回归。
- _suggest_layout -> _world_bbox 实装(3515);连带迁移 get_zone_map(376)/analyze_aesthetics(1366)/_analyze_group_layout(3278)。
- docs/testing.md + .gitignore 已入 commit;commit message reword 后与实物逐条吻合。

### 验收重跑(同审计口径)
- compileall OK; wheel 100,583B; stdio init 2.14.7 / tools=18 / list_sessions / ping OK。
- pytest 335 passed + 2 skipped;-m mayapy 收集 2 项 skip(exit 0)。
- ruff: src 实测 185(报 187,保守)/ 全仓 244(报 ~246);基线 188/288 -> 改善。
- mypy 227(基线 277)。

### 残留(登记,转后续轮次)
1. _suggest_layout pair-window(i+20)截断仍未披露(constraints/overlaps 已补 pair_window/pairs_skipped)。
2. checked/skipped 披露字段三处异构未统一。
3. AGENTS.md tests/ 结构表未含新文件;orbit_cam/shot_cam 默认名无 CAM_ 前缀。
4. rev2 报告 ruff 口径混用(errors vs wc -l 行数),实测更优、方向诚实。
