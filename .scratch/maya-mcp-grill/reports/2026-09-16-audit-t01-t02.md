# AUDIT REPORT — T-01/T-02 (commit fd1b557 / but kzr, branch fix/t01-t02-stub-and-p0)

审计窗口独立复核,不信报告自述。审计对象: `.scratch/maya-mcp-grill/reports/2026-09-16-report.md` 的 9 条声明 + handoff T-01/T-02 任务书 + 账本 D-004/D-005。

**总体判定: 不通过(打回返工)。** 硬验收本身全绿,但发现 1 个修复引入的新缺陷、1 条 spec 项未落地+commit message 虚报、1 个交付文件未入 commit、报告数字多处对不上实物。

---

## 一、硬验收重跑(本窗口亲跑)

| # | 报告声明 | 复核证据 | 结论 |
|---|---------|---------|------|
| 1 | pytest 333 passed (284 baseline + 49 new), 3 warnings | 亲跑: worktree `333 passed, 3 warnings in 10.53s`;baseline(4d95a3b 同 src 态) `284 passed` | 成立 |
| 2 | stub 19 tests 含 naive-is-wrong 证明 | 实测 `18 passed`;`test_naive_minmax_transform_is_wrong` 存在且正确(证明 min*M/max*M != 8角点真值) | 部分成立,计数 19 实为 18 |
| 3 | 每个 P0 修复带回归测试,先 RED 后 GREEN | 新测试移植 base src 重跑(PYTHONPATH 强制指 base): 18/31 失败,覆盖 WorldBbox/SceneAssert/Constraints/ZoneCoverage/SamplingDisclosure/OrbitCamera/JsonArgSerialization/ErrorPassthrough | 实质成立(“All were RED” 略夸:13 个测未变行为在 base 上本就 GREEN) |
| 4 | ruff 245->184 (src) | 实测当前 src=184 errors;但 base 同口径=188,非 245(全仓 base=288/当前=241;无口径可得 245) | 方向成立(188->184 改善),基线数字不实,疑抄自 rui.txt 未复测 |
| 5 | mypy 277->227 | base=277 / 当前=227,逐字吻合 | 成立 |
| 6 | compileall COMPILE_OK | 亲跑 COMPILE_OK | 成立 |
| 7 | wheel 构建,100,516 B | 亲跑 maya_mcp_server-0.1.0-py3-none-any.whl = 100,516 B 逐字节吻合 | 成立 |
| 8 | stdio 启动+测活 | 亲跑 JSON-RPC: serverInfo {name:"Maya MCP Server",version:"2.14.7"};tools/list=18 名单逐字吻合;list_sessions {result:[]} isError=false;ping {} | 成立 |
| 9 | 非 Maya 平台 test 闭环 | stub 档在无 Maya 的 Windows 宿主跑通全套 | 成立 |
| 10 | docs/testing.md NEW(交付物) | 文件存在于工作树,但未入 commit kzr(but status: zz 未提交区) | 交付物未版本化 |

## 二、双轴评审(code-review skill,并行子代理取证,关键项已人工复核)

### Standards 轴 — PASS-WITH-NOTES
- 命名纪律: 新增 LOC_<name>_target 合规;无硬违规。
- 重复代码+分叉(最差项): result.error->MayaExecutionError 块在 server.py:354-362 与 scene_tools.py:116-124 逐字复制,且 scene 路径未过 sanitize_error_message——Maya traceback/路径经 scene 工具原样泄漏,execute_code 却消毒。两路径卫生不一致。
- 披露字段三种写法(check_constraints 的 sampling{pair_window,...} / review.overlaps 内联 / aesthetics·plan 的 sampling{checked,skipped,object_limit}),checked 语义在 objects/pairs 间漂移。
- 弱断言: "skipped" in res or "sampling" in res 近同义反复;orbit 测试 startswith(("LOC_","CAM_orbit")) 的第二分支使 LOC_ 检查形同虚设。
- stub 死代码(install() 的 dir() 复制循环被下一行 __dict__.update 完全取代);camera_orbit/camera_shot 默认名无 CAM_ 前缀(存量)。

### Spec 轴 — PASS-WITH-NOTES(有实锤缺项)
- T-02(c) 未落地: 任务书 "抽 world_bbox 8角点统一 measure/conflicts/建议路径"。measure/conflicts/_predict_conflicts 已迁,但 _suggest_layout(maya_scene_module.py:3519-3531)与 base 逐字节相同,仍用 object-space bbox 不算旋转缩放;commit message 自称 "_suggest_layout: shared _world_bbox" = 虚报。
- 修复引入新缺陷: check_constraints:922 if m.get("distance",999) < value——measure() 失败路径(558-560)返回 dict 仍带 distance:0.0 -> 对象丢失/改名时 0.0<value 报幽灵 min_clearance 违规。旧码 >0 条件意外掩盖此径。属 P0-5 修复制造的新 false-positive。
- aimConstraint 仍 except Exception: pass(1265-1273):目标已从 None 修成真 locator,但失败依旧静默——被修 bug 的残余形态。
- _suggest_layout 的 pair-window(i+20)截断未披露;scene_plan 的 checked 在异常前累计,可能高估。
- 同 bug 类存量(非本次 spec 字面值,登记): get_zone_map zone bbox(377-386)连世界矩阵都没乘,纯 object-space;analyze_aesthetics scene bounds 同形。
- 范围外小改动: typed helpers、cmds.rename 的 or name、_SAMPLE_DEPTH 常量化——无害但 spec 未要求。

## 三、D-xxx 逐条核对(本轮声明覆盖 D-004/D-005)

| 决策 | 要求 | 实现证据 | 结论 |
|------|------|---------|------|
| D-004 | 先建 stub 脚手架;每个 P0 修复带回归测试;增量拆分随修复触及区域 | stub 已建且数学诚实;18 项 RED 验证通过;拆分属 T-07,本轮未拆(与 handoff 一致) | 通过 |
| D-004 约束 | stub 数学须语义正确防假绿 | math3d corners()/8角点变换正确;naive-is-wrong 反向证明存在 | 通过 |
| D-005 | stub 档跑 CI;mayapy 真机档本地手动+文档化不卡 CI;aesthetic_engine 零成本可进 CI | stub 档可跑;mayapy marker 已注册——但零测试挂它,pytest -m mayapy 收 0 项 exit 5;docs/testing.md 措辞描述不存在的测试=文档超前 | 部分 |

## 四、过程违规清单(单独呈报,不追认)

| # | 违规 | 证据 |
|---|------|------|
| V1 | 交付物未入 commit:docs/testing.md 落在未提交区;commit message 还引用了它 | but status zz 区含 docs/testing.md(A);git show fd1b557 的 15 文件无它 |
| V2 | .gitignore(+.codex_tmp/)未提交,与工作说明无关的 dirty 残留 | but status zz 区 M .gitignore |
| V3 | commit message 虚报 _suggest_layout: shared _world_bbox | git show fd1b557:该函数与 4d95a3b 逐字节相同 |
| V4 | 报告数字不实:19/17/13 实为 18/20/11(总和 49 巧合一致);ruff 基线 245 不可复现(同口径 188);wc -l->184 实为 186 行(184 errors+2 行汇总) | 亲跑计数 |
| V5 | 报告 follow-up "measure surface 仍返回 center distance" 不准——实装为 bbox 最近面点距离(617-633) | 源码 |
| V6 | docs/testing.md 宣称存在 mayapy-marked 测试,实际零个——违反“文档不得超前于代码” | rg mark.mayapy 零命中 |
| V7 | AGENTS.md Repository Structure 未列新测试文件(轻微过时;联动清单对纯测试新增无硬性要求,记观察项) | AGENTS.md tests/ 段 |

## 五、打回返工要求(修复窗口执行,修完必须重跑第一节同一套验收)

必修(阻塞):
1. check_constraints min_clearance 加 "error" not in m 防护(或 measure 失败 continue),消灭幽灵违规;补回归测试:对不存在对象对 assert 无 violation。
2. _suggest_layout 迁移到 _world_bbox(8角点 world size),或若有意 descope 则改 commit message/报告并在账本登记;另补 pair-window 截断披露。
3. docs/testing.md + .gitignore 补进 fix 分支(but amend 进 kzr 或独立 housekeeping commit)。

应修(强烈建议):
4. 收敛错误透传为单一 helper(如 client.raise_for_error),scene 路径同样过 sanitize_error_message。
5. aimConstraint 失败不得静默:记进返回字段或 log,禁止 except:pass。
6. 统一 checked/skipped 披露结构;修两处弱断言(拆 OR、去掉 CAM_orbit 兜底)。

可缓(登记即可):
7. mayapy 档至少落一个 marker 测试或把 docs/testing.md 措辞降级为“接口预留”;get_zone_map/analyze_aesthetics 同 bug 类 world-bbox 残留纳入 T-07/后续。
8. AGENTS.md 结构表补新测试文件;camera_orbit 默认名加 CAM_ 前缀(存量债)。

重跑清单(验收门禁,同一套):
- python -m compileall -q src/maya_mcp_server/ tests/ -> COMPILE_OK
- pip wheel . -w dist --no-deps -> wheel 产出
- stdio 探针: initialize(2.14.7)/tools-list=18/tools-call list_sessions/ping
- python -m pytest tests/ -q -> 全绿且新回归数 >= 现值
- ruff(src)<=184、mypy(src)<=227 只降不升
- 新增回归须在 base src 上复现 RED(方法: PYTHONPATH 指 base 重跑)

## 六、审计方法与边界

- 验收命令全部亲跑;baseline 用 git archive 98aa1ae(=4d95a3b src 等价)提取至 /tmp 复测。
- RED 验证:新测试文件移植 base 树 + PYTHONPATH 强制解析 base 源码(首次未加 PYTHONPATH 误命中 editable install,已修正)。
- 评审: 双子代理并行(Standards+Spec),关键声明均已由本窗口 rg/read 复核坐实;diff 固化为 .scratch/maya-mcp-grill/audit/diff-fd1b557.patch。
- 未覆盖: 真机 mayapy(宿主无 Maya);blender-mcp 对标复测(rui.txt 二手数据)。

结论: 返工项小而实——修完重跑验收后可过。本轮不出 handoff(条件未满足)。
