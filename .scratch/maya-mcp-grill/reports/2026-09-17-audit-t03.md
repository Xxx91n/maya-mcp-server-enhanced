# AUDIT — T-03 checkpoint/rollback（commit a2c8b3b/lkt + a651da7/sqx，branch fix/t03-checkpoint-rollback）

> 审计日期: 2026-09-17 · 审计窗口: 只出报告不动手 · 复核对象: reports/2026-09-16-report-t03.md + handoffs/next-round.md(rev4) + %TEMP%/handoff-t03-done.md

## 判定: 通过（带观察项，无阻断）

硬验收重跑全绿且与报告逐字吻合；10 条交付声明全部有实物证据；D-007/D-015/D-016 逐条落地。以下为观察项（弱化/口径问题），均不阻断，归属见末节。

## 1. 硬验收重跑（亲自执行，不信自述）

| 项 | 报告声明 | 审计实测 | 结论 |
|----|---------|---------|------|
| 编译 | compileall OK（存量 SyntaxWarning） | `python -m compileall -q src tests` exit 0 | ✅ |
| 打包 | wheel maya_mcp_server-0.1.0 | `pip wheel . --no-deps -w dist_audit` → whl 产出 | ✅ |
| 启动测活 | init→2.14.2/proto 2024-11-05；18 tools；ping {} | init→**2.14.7**/2024-11-05；18 tools；ping {}；scene_checkpoint 含 name/overwrite，scene_rollback 含 filename/discard_current_state | ✅（报告版本号笔误 2.14.2→实际 2.14.7，装饰性） |
| 测试 | 369 passed + 3 skipped | 369 passed, 3 skipped, 14.26s | ✅ |
| ruff | src=185 / 全仓=244（不升） | Found 185 / Found 244 | ✅ |
| mypy | 227→223 | Found 223 errors in 4 files | ✅ |

## 2. 声明→证据→结论 对照表

| # | 报告声明 | 实物证据 | 结论 |
|---|---------|---------|------|
| 1 | checkpoint=exportAll 内存态快照（P0-3 根除） | maya_scene_module.py:1086-1102 `_write_snapshot` 用 `cmds.file(exportAll=True,type="mayaAscii",force=True,prompt=False)`；src/ 无 copy2 残留（仅 security.py 模式黑名单提及 shutil）；base(fbdd518) 确为 copy2 → 测试真 RED 可复现 | ✅ |
| 2 | rollback=S2 重绑+结构化场景身份 | :1361 open→:1371 `rename=scene_before`；返回 scene_name_before/after、original_file_status(saved\|no_original_file)、scene_rebound_to（:1383-1392）；_rb_error(:1135) 统一结构化错误 | ✅ |
| 3 | 覆盖路径前必有内存态快照；失败中止+discard 逃生 | _auto_snapshot(:1111) exportAll+ts 撞名后缀；:1344-1358 失败→aborted=True；discard→safety_snapshot="skipped_by_user"；auto 成功+discard→warning | ✅ |
| 4 | name 白名单+同名拒绝/覆盖保留 | :1008 `^[A-Za-z0-9_-]+$`；:1191 拒绝带 suggestion（_suggest_name :1105 不自动采用）；:1207-1220 overwrite→prev_{ts}_{file}，preserved_as 披露 | ✅ |
| 5 | untitled→ad-hoc（D-015/016） | :1177-1186 默认报错+自愈 suggestion；:1202 cp_adhoc_{name}.ma；_checkpoint_dir(:1037) untitled→workspace/checkpoints，不可解析→结构化错误 | ✅ |
| 6 | 隐藏案例 1-4 有测试 | test_checkpoints_path_is_file/dir_unwritable（预检 :1066-1083）；test_same_name_rejected/overwrite_preserves；test_rollback_missing/corrupt_snapshot（exists+:1330-1340 //Maya ASCII 头校验报“快照已丢失”）；test_rollback_open_failure_reports_state（scene_name_after+scene_snapshot 建议 :1363-1367） | ✅（测试强度见观察项 O5） |
| 7 | '..' 穿越修复 | :1009 `^(cp\|prev)_[A-Za-z0-9_-]+\\.ma$` + :1312 basename + :1318 commonpath；test_rollback_rejects_traversal 覆盖 ../x.ma、..\\x.ma、sub/cp_x.ma、cp_../../x.ma | ✅ |
| 8 | prompt=False + absoluteName 消歧 | 全部 mutating 调用带 prompt=False（:1096,1361,1371）；_scene_file_path(:1014) expandName 优先+sceneName 兜底+normpath/expandvars | ✅（expandName vs spec 字面 absoluteName：语义正确、措辞漂移，观察项 O6） |
| 9 | 工具面+文档同步 | scene_checkpoint(name=None,overwrite=False)/scene_rollback(filename,discard_current_state=False)（scene_tools.py:552-631）；server.py:61-63,88-89 instructions；README×2 无“自动回滚”虚词；AGENTS.md tests 表补 test_checkpoint_rollback+test_mayapy_smoke；ledger D-016 current；ADR-0004 refined-by+Consequences；CONTEXT.md ad-hoc 术语；.gitignore 增 dist_audit/ | ✅ |
| 10 | stub 升级 | cmds.py:177 file() exportAll 写真件（//Maya ASCII 头+JSON）/open 校验头+restore/坏件抛错/expandName/absoluteName/workspace()；scene.py serialize/restore/workspace_dir；test_maya_stub 仍绿 | ✅ |

测试计数核对：test_checkpoint_rollback.py 实测 31 def test_；test_scene_tools_json.py 新增 3（overwrite/discard kwarg 透传、name=None 默认）；test_mayapy_smoke.py +1 reference-edit 往返（pytestmark=mayapy，诚实标记未实跑）。335+34=369 ✅。

## 3. D-xxx 逐条核对

- **D-007**：exportAll 真快照 ✅；basename 白名单 ✅；S2=open+rename ✅；auto_before_rollback 不变式 ✅；四字段结构化 ✅；文档两诚实边界：undo 边界全表面覆盖（README×2/工具 docstring/server instructions/CONTEXT），**references 展平边界仅落 CONTEXT.md+ADR+工具 docstring，未上 README/server instructions（观察项 O3）**。
- **D-015**：untitled 默认报错+显式 name→ad-hoc ✅；白名单拒绝+suggestion 不自动采用 ✅；auto 失败中止+discard 逃生+skipped_by_user+warning ✅；隐藏案例 1-4 ✅；absoluteName 消歧（实现=expandName，语义正确）✅；prompt=False ✅；**单会话假设仅落 ADR+模块注释+scene_checkpoint docstring，未上 README/CONTEXT（观察项 O3）**。
- **D-016**：(a) ad-hoc 落 <workspace>/checkpoints 且 cp_adhoc_ 前缀 ✅；(b) cp_{name}.ma + prev_{ts}_{file} 经 preserved_as 披露可 rollback ✅（prev_ 匹配文件白名单，test_rollback_to_preserved_prev_file 实证）；(c) cp_auto_before_rollback_{ts}[_{n}] ✅；(d) **untitled+auto 组合代码路径成立（workspace dir→auto 快照）但无专用测试钉住落点、docstring 未显式声明——弱化项 O4**；(e) 文件名白名单+basename+commonpath ✅；(f) original_file_status 值域+scene_rebound_to 语义 ✅。

## 4. 双轴评审结论（code-review skill）

**Standards 轴**：联动规范/测试纪律/commit message 字字为真 全合规；无硬性违规。judgment-call 级：函数内局部 import×10（与文件既有约定一致，不判）；"saved" if/else×3、测试 boom() 闭包×3 轻度重复；错误 schema 异构（raw dict vs _rb_error）——对应未决 spec#4，仅观察。

**Spec 轴**：无 missing 级缺口；scope creep 仅披露性额外表字段（object_count/size_mb 等，无害）；需记录：prev_ 可 rollback✅、mutating-only prompt=False 口径✅。

## 5. 观察项（弱化/口径，非阻断）

- O1 报告笔误：stdio 版本自述 2.14.2，实测 2.14.7（rev2 审计同为 2.14.7）。
- O2 报告“+34 新用例”与“31+3+1”枚举含糊：实际 34 新增 passed + 1 新增 skipped，可 reconcile。
- O3 诚实边界/单会话假设用户面覆盖不全：references 展平、单会话假设未上 README/server instructions（落点=CONTEXT/ADR/docstring/注释）。天然归宿=T-09 README 逐项核对。
- O4 D-016d untitled+auto 组合：实现正确但无测试钉住 auto 快照落 workspace、无 docstring 显式声明。建议 T-04 顺带补 1 个断言（不返工）。
- O5 隐藏案例 3 测试只断言 error 含 "snapshot"，未钉“快照已丢失”字面量与 missing/corrupt 文案区分。
- O6 spec 字面 absoluteName vs 实现 expandName——语义正确，措辞漂移。
- O7 prev_{ts} 秒级时间戳同秒双覆盖会静默丢首个保留件（os.replace 覆盖同名 prev）——spec 未覆盖，边缘。
- O8 rev4 handoff 登记债含两条陈旧项："dist_audit/ 未 gitignore"（本轮已偿还）与 "AGENTS.md tests/ 结构表未含新文件"（本轮已偿还一半，orbit_cam CAM_ 前缀仍在）。
- O9 存量陈旧（非本轮引入）：AGENTS.md "15 MCP scene tool definitions"（实 13）；README 特性表缺 scene_checkpoint_list 行。归 T-09。

## 6. 过程合规

- 账本纪律：D-016 落账 current ✅；文档纪律：同 commit 同步 ✅；commit message 字字为真 ✅。
- 基线口径诚实：ruff/mypy 只降不升 ✅；mayapy 档诚实标注未实跑 ✅。
- 未发现过程违规；版本控制痕迹与 but 工作区一致（gitbutler/workspace）。

## 7. 处置建议

不打回返工。O4/O5 建议并入 T-04 顺带补钉；O3/O9 归 T-09 文档真实性问题域（已在其 spec 内）。
